# 开启 Lombok 配置继承（必须加）
config.stopBubbling = false

# 所有类的 toString() 默认 callSuper = true
lombok.toString.callSuper = call

# 所有类的 equals/hashCode 默认 callSuper = true
lombok.equalsAndHashCode.callSuper = call
